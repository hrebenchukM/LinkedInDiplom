# Real backend mode (local dev). Restart `npm run dev` after changes.
# Vite loads this file automatically; .env.example is NOT loaded.

VITE_USE_MOCK_AUTH=false
VITE_ENABLE_GUEST=false
VITE_API_BASE_URL=
VITE_DEV_PROXY_TARGET=https://localhost:7011